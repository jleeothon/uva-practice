// 10189 - Minesweeper
#include <stdio.h>
#include <string.h>

int rows, cols;
int i, j;

int main() {
	int field[101][101];
	while (scanf("%d %d\n", &rows, &cols) != EOF) {
		memset(field, 0, sizeof(int) * rows * cols);
		for (i = 1; i < rows; i++) {
			for (j = 1; j < cols; j++) {
				getc
			} // end for j
		} // end for i
	}
	return 0;
}