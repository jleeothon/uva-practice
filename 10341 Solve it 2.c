#include <stdio.h>
#include <math.h>
#include <stdlib.h>
d

double p, q, r, s, t, u;

double f(double x) {
	return p*exp(-x) + q * sin(x) + r*cos(x) + s*tan(x) + t*x*x + u;
}

double find(double max, double min) {
	return mid;
}


int main() {
	
	return 0;
}