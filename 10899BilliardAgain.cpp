// 10899 - Billiard Again

float a, b, v, A, s, x, y; 

struct segment {
	float start;
	float end;
	float closestDistance;
	
};

int main() {

	while (scanf("%f %f %f %f %f %f %f\n", &a, &b, &v, &A, %s, &x, &y) != EOF) {
		//
	}

	return 0;
}