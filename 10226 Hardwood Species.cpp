#include <stdio.h>
#include <map>
#include <string>
#include <iostream>

using namespace std;

int t;
string name;

int main() {
	cin >> n;
	cin >> name;
	cin >> name;
	while (t--) {
		std::map<string, int> tree_numbers();
		cin >> n;
		getline(cin, name);
		if (name.compare("")) {
			goto solve;
		}


		solve:
		printf("\n");
	}
	return 1;
}